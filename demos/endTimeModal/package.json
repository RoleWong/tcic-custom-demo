{
  "name": "white-board-auth-demo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env NODE_ENV=development rspack serve",
    "build": "cross-env NODE_ENV=production rspack build"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "@rspack/cli": "^1.1.4",
    "@rspack/core": "^1.1.4"
  }
}